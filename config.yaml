vault_path: "vault" # Relative to project root
llm:
  selector_model: "claude-3-haiku-20240307"
  chat_model: "claude-3-sonnet-20240229"
  max_tokens: 150000

# Files to always include in the context, regardless of the query.
always_include:
  - "project_goals.md"

vault_extensions:
  - ".md"
exclude_patterns:
  - ".obsidian" 